# Expose collector heartbeat in Grid

No frontmatter here. Body is the description.

The collector emits a heartbeat every 30 s; Grid tiles should go yellow after
60 s of idle, grey after 90 s of missing heartbeats.
